# Licensed local fonts

Future licensed `.woff2` files belong here and must be registered through `next/font/local`. Theme and template data references stable IDs from `src/lib/font-registry.ts`, never arbitrary font-family strings.
